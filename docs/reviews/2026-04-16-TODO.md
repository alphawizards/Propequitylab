# PropEquityLab — Master Issue List
**Compiled:** 2026-04-16  
**Sources:** Security · Code Quality · API Design · Test Coverage · Frontend UI · Performance

---

## CRITICAL — Fix Before Next Deploy

| # | Area | Issue | File(s) |
|---|---|---|---|
| C1 | Security/API | `POST /api/plans/project` is unauthenticated — DoS via unbounded loop, public attack surface | `backend/routes/plans.py:266` |
| C2 | Security | GDPR delete-account soft-deletes user row only — all financial data persists. No hard-delete scheduled job exists. Response still references `zapiio.com` | `backend/routes/gdpr.py:295-317` |
| C3 | Code/API | GDPR export endpoint 500s on every call — references 8 field names that no longer exist on models (`a.asset_type`, `a.value`, `l.liability_type`, `l.amount`, `i.source_name`, `i.income_type`, `e.description`, `prop.land_value`) | `backend/routes/gdpr.py:128-170` |
| C4 | API | Route ordering conflict — `GET /projections/portfolio/{id}` unreachable (wildcard `/{property_id}` registered first). Same bug in loans and valuations | `backend/routes/projections.py:223,290` · `loans.py` · `valuations.py` |
| C5 | Security | `deep_copy_portfolio` and `delete_scenario` sub-table queries have no `user_id` filter — latent IDOR if outer ownership check ever bypassed | `backend/routes/scenarios.py:113-285,478-500` |
| C6 | Frontend | `Settings.jsx` uses `toast` from `'sonner'` but no `<Toaster>` mounted — all settings feedback (save, export, delete account) silently does nothing | `frontend/src/pages/Settings.jsx` |
| C7 | Frontend | Dead `Dashboard.jsx` + `mockData.js` routed nowhere but compiled into production bundle — also pulls in 6 dashboard components and 2 modals unnecessarily | `frontend/src/pages/Dashboard.jsx` · `mockData.js` |

---

## HIGH — Next Sprint

| # | Area | Issue | File(s) |
|---|---|---|---|
| H1 | Performance | N+1 query in `get_portfolio_projections` — 6 SQL queries per property in a Python loop. 10 properties × 10-year projection = 61 queries | `backend/routes/projections.py:351` |
| H2 | Performance | Dashboard summary makes 5 separate queries + aggregates in Python, iterating assets list 7+ times. Push to Postgres `func.sum()` | `backend/routes/dashboard.py:77` |
| H3 | Performance | `DashboardSummary` uses `float` for all monetary fields — silently discards `Decimal` precision. Same issue in `ProjectionResult`/`ProjectionYear` | `backend/routes/dashboard.py:31` · `plans.py` |
| H4 | Security | Financial amounts passed as URL query params — appear verbatim in Railway + Cloudflare logs | `backend/routes/loans.py:206-248` |
| H5 | Security | JWT audience verification disabled (`verify_aud: False`) — tokens from other Clerk apps accepted | `backend/utils/clerk_auth.py:108` |
| H6 | Security | Loan amounts accepted as `float` not `Decimal` — violates `DECIMAL(19,4)` rule | `backend/routes/loans.py:206,240` |
| H7 | Security | `compare_scenario` fetches source portfolio without `user_id` filter | `backend/routes/scenarios.py:537-544` |
| H8 | API | No pagination on any list endpoint — unbounded queries against Neon serverless | All `GET` list routes |
| H9 | API | All DELETE handlers return 200 + body instead of 204 No Content | `portfolios.py`, `properties.py`, `loans.py`, `scenarios.py`, etc. |
| H10 | Code | `_provision_user` has 3 separate `session.commit()` calls — user creation non-atomic; crash between commits leaves orphaned rows | `backend/routes/` (onboarding/webhooks) |
| H11 | Code | All route files import `get_current_user` from `clerk_auth` directly, bypassing `utils.auth` abstraction — breaks rollback path | All 16 route files |
| H12 | Frontend | Zero route-level code splitting — every page in initial bundle | `frontend/src/App.js` |
| H13 | Frontend | `PropertyFormModal` has no validation — empty `purchase_price` submits as `0` | `frontend/src/components/properties/PropertyFormModal.jsx` |
| H14 | Frontend | Mobile navigation absent below `md` breakpoint — sidebar hides with no replacement | `frontend/src/components/Sidebar.jsx` |
| H15 | Test | 8 `legacy_test_` files skipped by pytest — cover portfolios, properties, income, expenses, assets, liabilities, plans, dashboard | `backend/tests/legacy_test_*.py` |
| H16 | Test | Zero HTTP-level tests for: dashboard (3 routes), scenarios (6 routes), projections (3 routes), loans (7 routes), valuations (5 routes), plans, gdpr, onboarding | `backend/routes/` |
| H17 | Test | `backend_test.py` is a manual requests script against a hardcoded URL — not runnable by pytest, should be deleted or moved to `scripts/` | `backend/backend_test.py` |

---

## MEDIUM — Address This Month

| # | Area | Issue | File(s) |
|---|---|---|---|
| M1 | Performance | No `pool_recycle` set — Neon drops idle connections; adds `SELECT 1` round-trip to every affected request. Pool size (10+20) too large for single Railway worker | `backend/utils/database_sql.py:62` |
| M2 | Performance | Missing composite `(portfolio_id, user_id)` indexes on Property, Asset, Liability, IncomeSource, Expense | DB migrations |
| M3 | Performance | `PortfolioContext` refetches summary on every `currentPortfolio` change with no deduplication or in-flight guard | `frontend/src/context/PortfolioContext.jsx:45` |
| M4 | Performance | `generate_portfolio_projections` recalculates amortisation from scratch for every year — `Decimal ** int` with n=360 expensive; memoize `(1+r)^n` per loan | `backend/utils/calculations.py:705` |
| M5 | Performance | Dashboard snapshot re-runs all 5 dashboard queries via `get_dashboard_summary()` — data just computed to render | `backend/routes/dashboard.py:246` |
| M6 | Code | `_verify_property_access` duplicated verbatim in `loans.py` and `valuations.py` — extract to shared utility | `backend/routes/loans.py` · `valuations.py` |
| M7 | Code | 3 separate inline frequency-conversion implementations instead of using `annualize_amount` from `calculations.py`. `to_monthly` dict in `dashboard.py:156` diverges from canonical `FREQUENCY_MULTIPLIERS` | `backend/routes/dashboard.py:156` |
| M8 | Code | `monthly_rate` local variable in `calculate_remaining_balance` shadows module-level function of same name | `backend/utils/calculations.py` |
| M9 | Code | `load_demo_data` and `seed_sample_data` are ~300 lines of near-identical seeding logic — unify into one | `backend/routes/onboarding.py` |
| M10 | API | All `PUT` endpoints implement partial updates (`exclude_unset=True`) — `PATCH` is the correct method | All update routes |
| M11 | API | Health check returns HTTP 200 even when DB is down — load balancers/Railway never trigger failover | `backend/server.py` |
| M12 | API | `GET /api/plans/{plan_id}/projections` requires `portfolio_id` as query param — already on `plan.portfolio_id`, resolve from model | `backend/routes/plans.py` |
| M13 | API | No API versioning — breaking changes have no migration path | `backend/server.py` |
| M14 | Frontend | Dark mode `dark:` variants in only 2 of 11 page files — `DashboardNew`, `PropertiesPage`, `PlansPage`, `ProjectionsPage`, `Settings` have zero coverage | `frontend/src/pages/` |
| M15 | Frontend | `UserContext` duplicates `user` object from `AuthContext` — split mutable state, stale-read risk | `frontend/src/context/UserContext.jsx` |
| M16 | Frontend | `NavItem` defined inside `Sidebar` — full remount on every parent render, resets `isOpen` state | `frontend/src/components/Sidebar.jsx` |
| M17 | Frontend | `PropertiesPage` and `ProjectionsPage` swallow fetch errors silently (`console.error` only) — users see empty list with no explanation | `frontend/src/pages/PropertiesPage.jsx` · `ProjectionsPage.jsx` |
| M18 | Frontend | `DashboardNew` fires 3 sequential fetches with independent loading states — skeleton drops prematurely while charts still load | `frontend/src/pages/DashboardNew.jsx` |
| M19 | Frontend | `PortfolioContext` and `UserContext` create new `value` object literal on every render with no `useMemo` — every consumer re-renders unnecessarily | `frontend/src/context/` |
| M20 | Security | `ProjectionInput` has no field bounds — authenticated users can trigger 10,000-iteration CPU loop | `backend/routes/plans.py:31-40` |
| M21 | Security | `generate_id()` uses 8-char UUID prefix — ~1% collision at 9,300 records | `backend/routes/scenarios.py:38` |
| M22 | Security | CORS origins logged at `INFO` on every cold start | `backend/server.py:57` |
| M23 | Security | `partner_details` stored as unvalidated raw JSON with no GDPR consent mechanism for third party | `backend/routes/onboarding.py:109-111` |
| M24 | Test | No frontend tests at all — zero `.test.jsx` or `.spec.js` files | `frontend/src/` |
| M25 | Test | `POST /valuations` silently mutates `property.current_value` — no test guards this side-effect | `backend/routes/valuations.py` |
| M26 | Test | `deep_copy_portfolio` copies 8 sub-tables — no test verifies FK mapping fidelity | `backend/routes/scenarios.py` |

---

## LOW — Backlog

| # | Area | Issue | File(s) |
|---|---|---|---|
| L1 | Security | CSP has `unsafe-inline` and `unsafe-eval` — track for nonce-based approach | `backend/server.py:150` |
| L2 | Security | Clerk webhook returns 500 when secret unconfigured — should be 503 for retry | `backend/routes/clerk_webhooks.py:79` |
| L3 | Security | `delete_portfolio` does not delete Loan/PropertyValuation/RentalIncome sub-records — orphan rows accumulate, compounds GDPR gap | `backend/routes/portfolios.py:169` |
| L4 | Security | Frontend Sentry sends user email + full name — reduce to `id` only | `frontend/src/utils/sentry.js:110` |
| L5 | Security | Health check discloses `"stack": "PostgreSQL + Railway"` to unauthenticated callers | `backend/server.py:99` |
| L6 | Security | Frontend Sentry `beforeSend` reads `localStorage.getItem('user')` — nothing writes this key, dead code, forwards emails if that changes | `frontend/src/utils/sentry.js:74-85` |
| L7 | API | Intentionally public endpoints (categories, types) not documented as such in OpenAPI — indistinguishable from accidentally unauthenticated | `expenses.py`, `assets.py`, `liabilities.py`, `plans.py` |
| L8 | API | No `response_model` on DELETE, snapshot, compare, and summary endpoints — absent from OpenAPI schema, no output validation | Multiple routes |
| L9 | Performance | Health check fires real DB query on every call — needs TTL-cached result | `backend/server.py` |
| L10 | Performance | `api.js` global 30s timeout too permissive for fast endpoints, too tight for projections | `frontend/src/services/api.js` |
| L11 | Code | `deep_copy_portfolio` does a `session.flush()` per loan inside loop to populate `loan_id_map` that is never used | `backend/routes/scenarios.py` |
| L12 | Code | `hasattr` guards in `onboarding.py` on known model fields are dead conditionals | `backend/routes/onboarding.py` |

---

## Summary

| Severity | Count |
|---|---|
| Critical | 7 |
| High | 17 |
| Medium | 26 |
| Low | 12 |
| **Total** | **62** |

### Top 3 by risk

1. **GDPR export 500s on every call** (C3) — active compliance endpoint returning 500 in production right now
2. **Unauthenticated computation endpoint** (C1) — DoS vector, fix is one line
3. **GDPR delete doesn't erase data + no scheduled job** (C2) — Privacy Act / GDPR Article 17 violation

### Review files
- `docs/reviews/2026-04-16-security-hardening.md`
- `docs/reviews/2026-04-16-code-quality.md`
- `docs/reviews/2026-04-16-api-design.md`
- `docs/reviews/2026-04-16-test-coverage.md`
- `docs/reviews/2026-04-16-frontend-ui.md`
- `docs/reviews/2026-04-16-performance.md`
